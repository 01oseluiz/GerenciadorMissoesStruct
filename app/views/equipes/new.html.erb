<% if pessoa_signed_in? and current_pessoa.tipo.downcase == "admin" %>

    <%= flash_mensagem %>

    <h1>Montar Equipe</h1>

    <div id="demais_form_box">
      <%= render 'form', equipe: @equipe %>
    </div>

    <%= link_to 'Voltar', pessoa_path(current_pessoa) %>
<% else %>
    <%= render 'main_page/erro_autenticar_admin' %>
<% end %>