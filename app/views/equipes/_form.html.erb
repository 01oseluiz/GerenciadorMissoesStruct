<%= form_with(model: equipe, local: true) do |form| %>
  <% if equipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipe.errors.count, "error") %> prohibited this equipe from being saved:</h2>

      <ul>
      <% equipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nomeEq %>
    <%= form.text_field :nomeEq, id: :equipe_nomeEq %>
  </div>

  <div class="field">
    <%= form.label :Membro_1 %>
    <%= form.select :mb_1_id, Pessoa.new.get_opcoes_pessoas %>
  </div>

  <div class="field">
    <%= form.label :Membro_2 %>
    <%= form.select :mb_2_id, Pessoa.new.get_opcoes_pessoas %>
  </div>

  <div class="field">
    <%= form.label :Membro_3 %>
    <%= form.select :mb_3_id, Pessoa.new.get_opcoes_pessoas %>
  </div>

  <div class="field">
    <%= form.label :Membro_superior %>
    <%= form.select :mb_S_id, Pessoa.new.get_opcoes_pessoas %>
  </div>

  <div class="actions">
    <%= form.submit "Enviar" %>
  </div>
<% end %>
