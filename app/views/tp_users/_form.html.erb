<%= form_with(model: tp_user, local: true) do |form| %>
  <% if tp_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tp_user.errors.count, "error") %> prohibited this tp_user from being saved:</h2>

      <ul>
      <% tp_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo, id: :tp_user_tipo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
